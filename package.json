{
    "name": "govaina",
    "private": true,
    "version": "0.0.0",
    "type": "module",
    "scripts": {
        "dev": "npm run dev --prefix client",
        "build": "npm run build:client --prefix client && npm run build:server --prefix client",
        "preview": "npm run preview --prefix client"
    },
    "dependencies": {
        "concurrently": "^8.0.2"
    },
    "devDependencies": {
    }
}